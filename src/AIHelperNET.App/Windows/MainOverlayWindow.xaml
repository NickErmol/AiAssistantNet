<Window x:Class="AIHelperNET.App.Windows.MainOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:AIHelperNET.App.Windows.Controls"
        xmlns:controls="clr-namespace:AIHelperNET.App.Controls"
        Title="AIHelper"
        Width="600" Height="640"
        MinWidth="320" MinHeight="320"
        WindowStyle="None"
        Background="{DynamicResource Brush.Background.Window}"
        Opacity="0.75"
        Topmost="True"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">

    <DockPanel>
        <!-- Title bar -->
        <Border DockPanel.Dock="Top"
                Background="{DynamicResource Brush.Background.TitleBar}"
                Padding="8,4"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="▶"
                        Style="{StaticResource IconBtn}"
                        FontSize="9"
                        AutomationProperties.AutomationId="Btn_ToggleSidebar"
                        Visibility="{Binding SessionControl.IsSidebarVisible,
                            Converter={StaticResource InverseBoolToVisibilityConverter}}"
                        Command="{Binding SessionControl.ToggleSidebarCommand}"
                        Margin="0,0,6,0"/>

                <TextBlock Grid.Column="1"
                           AutomationProperties.AutomationId="Header_StatusText"
                           Foreground="{DynamicResource Brush.Foreground.Secondary}"
                           FontSize="{DynamicResource Font.SM}"
                           VerticalAlignment="Center">
                    <Run Text="AIHelper · "/>
                    <Run Text="●"
                         Foreground="{Binding SessionControl.IsSessionActive,
                             Converter={StaticResource BoolToColorConverter},
                             ConverterParameter='#44FF88|#666666'}"/>
                    <Run Text=" "/>
                    <Run Text="{Binding SessionControl.IsSessionActive,
                        Converter={StaticResource BoolToStringConverter},
                        ConverterParameter='Listening|Stopped'}"/>
                </TextBlock>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="{Binding SessionControl.IsSessionActive,
                                Converter={StaticResource BoolToStringConverter},
                                ConverterParameter='Stop|Start'}"
                            AutomationProperties.AutomationId="Btn_ToggleSession"
                            Command="{Binding SessionControl.ToggleSessionCommand}"
                            Style="{StaticResource ActionBtn}"/>
                    <Button x:Name="StealthBtn"
                            Content="🎥"
                            AutomationProperties.AutomationId="Btn_ToggleStealth"
                            Style="{StaticResource IconBtn}"
                            FontSize="13"
                            Click="ToggleStealth_Click"
                            ToolTip="Toggle screen capture visibility"/>
                    <Button Content="◐"
                            AutomationProperties.AutomationId="Btn_ToggleTheme"
                            Style="{StaticResource IconBtn}"
                            FontSize="13"
                            Click="ToggleTheme_Click"
                            ToolTip="Toggle theme"/>
                    <Button Content="&#128203;"
                            AutomationProperties.AutomationId="Btn_ToggleHistory"
                            Style="{StaticResource IconBtn}"
                            FontSize="12"
                            Click="ToggleHistory_Click"
                            ToolTip="Session history"/>
                    <Button Content="⚙"
                            AutomationProperties.AutomationId="Btn_OpenSettings"
                            Style="{StaticResource IconBtn}"
                            FontSize="11"
                            Click="OpenSettings_Click"/>
                    <Button Content="−"
                            Style="{StaticResource IconBtn}"
                            FontSize="16"
                            Click="Minimize_Click"
                            ToolTip="Minimize (Ctrl+Shift+H to restore)"/>
                    <Button Content="✕"
                            Style="{StaticResource IconBtn}"
                            FontSize="11"
                            Foreground="#CC4444"
                            Margin="4,0,0,0"
                            Click="Close_Click"
                            ToolTip="Close"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Sidebar -->
        <Border DockPanel.Dock="Left"
                AutomationProperties.AutomationId="Sidebar"
                Width="{Binding SessionControl.IsSidebarVisible,
                    Converter={StaticResource BoolToWidthConverter},
                    ConverterParameter='120'}"
                Background="{DynamicResource Brush.Background.Sidebar}"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="0,0,1,0"
                Padding="8">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionLabel}" Text="MODE" Margin="0,0,0,4"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Audio Only"
                             GroupName="Mode"
                             AutomationProperties.AutomationId="Mode_AudioOnly"
                             IsChecked="{Binding SessionControl.Mode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=AudioOnly}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Screen Only"
                             GroupName="Mode"
                             AutomationProperties.AutomationId="Mode_ScreenOnly"
                             IsChecked="{Binding SessionControl.Mode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=ScreenOnly}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Audio + Screen"
                             GroupName="Mode"
                             AutomationProperties.AutomationId="Mode_AudioAndScreen"
                             IsChecked="{Binding SessionControl.Mode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=AudioAndScreen}"/>

                <TextBlock Style="{StaticResource SectionLabel}" Text="AUDIO SOURCE"
                           Margin="0,10,0,4"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Mic Only"
                             GroupName="AudioSource"
                             AutomationProperties.AutomationId="AudioSource_MicOnly"
                             IsChecked="{Binding SessionControl.AudioSource,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=MicrophoneOnly}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="System Only"
                             GroupName="AudioSource"
                             AutomationProperties.AutomationId="AudioSource_SystemOnly"
                             IsChecked="{Binding SessionControl.AudioSource,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=SystemAudioOnly}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Both"
                             GroupName="AudioSource"
                             AutomationProperties.AutomationId="AudioSource_Both"
                             IsChecked="{Binding SessionControl.AudioSource,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=Both}"/>

                <TextBlock Style="{StaticResource SectionLabel}" Text="STATUS"
                           Margin="0,10,0,4"/>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <Ellipse Style="{StaticResource StatusDot}"
                             Fill="{Binding SessionControl.IsMicActive,
                                 Converter={StaticResource BoolToColorConverter},
                                 ConverterParameter='#44FF88|#444444'}"/>
                    <TextBlock AutomationProperties.AutomationId="StatusDot_Mic"
                               AutomationProperties.HelpText="{Binding SessionControl.IsMicActive,
                                   Converter={StaticResource BoolToStringConverter},
                                   ConverterParameter='active|inactive'}"
                               Text="Mic"
                               Foreground="{DynamicResource Brush.Foreground.Secondary}"
                               FontSize="{DynamicResource Font.XS}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <Ellipse Style="{StaticResource StatusDot}"
                             Fill="{Binding SessionControl.IsSystemAudioActive,
                                 Converter={StaticResource BoolToColorConverter},
                                 ConverterParameter='#44FF88|#444444'}"/>
                    <TextBlock AutomationProperties.AutomationId="StatusDot_System"
                               AutomationProperties.HelpText="{Binding SessionControl.IsSystemAudioActive,
                                   Converter={StaticResource BoolToStringConverter},
                                   ConverterParameter='active|inactive'}"
                               Text="System"
                               Foreground="{DynamicResource Brush.Foreground.Secondary}"
                               FontSize="{DynamicResource Font.XS}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <Ellipse Style="{StaticResource StatusDot}"
                             Fill="{Binding SessionControl.IsOcrReady,
                                 Converter={StaticResource BoolToColorConverter},
                                 ConverterParameter='#44FF88|#444444'}"/>
                    <TextBlock AutomationProperties.AutomationId="StatusDot_OCR"
                               AutomationProperties.HelpText="{Binding SessionControl.IsOcrReady,
                                   Converter={StaticResource BoolToStringConverter},
                                   ConverterParameter='active|inactive'}"
                               Text="OCR"
                               Foreground="{DynamicResource Brush.Foreground.Secondary}"
                               FontSize="{DynamicResource Font.XS}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,2">
                    <Ellipse Style="{StaticResource StatusDot}"
                             Fill="{Binding SessionControl.IsAiConnected,
                                 Converter={StaticResource BoolToColorConverter},
                                 ConverterParameter='#44FF88|#444444'}"/>
                    <TextBlock AutomationProperties.AutomationId="StatusDot_AI"
                               AutomationProperties.HelpText="{Binding SessionControl.IsAiConnected,
                                   Converter={StaticResource BoolToStringConverter},
                                   ConverterParameter='active|inactive'}"
                               Text="AI"
                               Foreground="{DynamicResource Brush.Foreground.Secondary}"
                               FontSize="{DynamicResource Font.XS}"/>
                </StackPanel>

                <TextBlock Style="{StaticResource SectionLabel}" Text="AUDIO LEVELS" Margin="0,10,0,4"/>
                <TextBlock Text="🎤" FontSize="{DynamicResource Font.XS}"
                           Foreground="{DynamicResource Brush.Foreground.Muted}"/>
                <ProgressBar Value="{Binding AudioLevel.MicLevel}"
                             Minimum="0" Maximum="1"
                             Height="6" Margin="0,1,0,4"
                             Background="{DynamicResource Brush.Background.Panel}"
                             Foreground="{DynamicResource Brush.Semantic.Active}"/>
                <TextBlock Text="🔊" FontSize="{DynamicResource Font.XS}"
                           Foreground="{DynamicResource Brush.Foreground.Muted}"/>
                <ProgressBar Value="{Binding AudioLevel.SystemLevel}"
                             Minimum="0" Maximum="1"
                             Height="6" Margin="0,1,0,0"
                             Background="{DynamicResource Brush.Background.Panel}"
                             Foreground="#44AAFF"/>

                <TextBlock Style="{StaticResource SectionLabel}" Text="FOLLOW-UPS"
                           Margin="0,10,0,4"/>
                <ToggleButton Content="Toggle Follow-ups"
                              IsChecked="{Binding ConversationTurn.IsFollowUpEnabled}"
                              Padding="5,2"
                              Margin="0,0,4,0"/>

                <TextBlock Style="{StaticResource SectionLabel}" Text="SCREEN" Margin="0,10,0,4"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="General"
                             GroupName="ScreenMode"
                             AutomationProperties.AutomationId="ScreenMode_General"
                             IsChecked="{Binding SessionControl.ScreenAnalysisMode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=General}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Solve coding"
                             GroupName="ScreenMode"
                             AutomationProperties.AutomationId="ScreenMode_SolveCoding"
                             IsChecked="{Binding SessionControl.ScreenAnalysisMode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=SolveCodingTask}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Debug error"
                             GroupName="ScreenMode"
                             AutomationProperties.AutomationId="ScreenMode_DebugError"
                             IsChecked="{Binding SessionControl.ScreenAnalysisMode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=DebugError}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Explain code"
                             GroupName="ScreenMode"
                             AutomationProperties.AutomationId="ScreenMode_ExplainCode"
                             IsChecked="{Binding SessionControl.ScreenAnalysisMode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=ExplainCode}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="System design"
                             GroupName="ScreenMode"
                             AutomationProperties.AutomationId="ScreenMode_SystemDesign"
                             IsChecked="{Binding SessionControl.ScreenAnalysisMode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=SystemDesign}"/>
                <RadioButton Style="{StaticResource RadioBtn}" Content="Multiple choice"
                             GroupName="ScreenMode"
                             AutomationProperties.AutomationId="ScreenMode_MultipleChoice"
                             IsChecked="{Binding SessionControl.ScreenAnalysisMode,
                                 Converter={StaticResource EnumToBoolConverter},
                                 ConverterParameter=MultipleChoice}"/>
                <CheckBox Content="+ interviewer (5 lines)"
                          IsChecked="{Binding SessionControl.IncludeInterviewerContext}"
                          Foreground="{DynamicResource Brush.Foreground.Secondary}"
                          FontSize="{DynamicResource Font.XS}"
                          Margin="0,4,0,0"/>
                <Button Content="📷 Capture"
                        AutomationProperties.AutomationId="Btn_Capture"
                        Style="{StaticResource ActionBtn}"
                        Margin="0,4,0,0"
                        Command="{Binding ConversationTurn.CaptureScreenCommand}"
                        CommandParameter="{Binding SessionControl}"/>

                <Button Content="◀ Hide"
                        AutomationProperties.AutomationId="Btn_HideSidebar"
                        HorizontalAlignment="Left"
                        Command="{Binding SessionControl.ToggleSidebarCommand}"
                        Style="{StaticResource IconBtn}"
                        Margin="0,12,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main content area: live view and history panel -->
        <Grid x:Name="ContentArea">
            <!-- Live session view -->
            <Grid x:Name="LiveView">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" MinHeight="60"/>
                    <RowDefinition Height="4"/>
                    <RowDefinition Height="*" MinHeight="80"/>
                </Grid.RowDefinitions>

                <!-- Transcript panel -->
                <Border Grid.Row="0" Background="{DynamicResource Brush.Background.Panel}"
                        DataContext="{Binding Transcript}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top"
                                   Style="{StaticResource SectionLabel}"
                                   Text="TRANSCRIPT"
                                   Margin="8,6,8,3"/>
                        <ScrollViewer x:Name="TranscriptScroll"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Margin="8,0,8,6">
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,2">
                                            <TextBlock Text="{Binding TimestampLabel}"
                                                       Foreground="{DynamicResource Brush.Foreground.Muted}"
                                                       FontSize="{DynamicResource Font.XS}"
                                                       Margin="0,0,6,0"/>
                                            <TextBlock Text="{Binding SpeakerLabel}"
                                                       Foreground="{Binding SpeakerColor}"
                                                       FontSize="{DynamicResource Font.SM}"
                                                       FontWeight="SemiBold"
                                                       Margin="0,0,4,0"/>
                                            <TextBlock Text="{Binding Text}"
                                                       Foreground="{DynamicResource Brush.Foreground.Secondary}"
                                                       FontSize="{DynamicResource Font.SM}"
                                                       TextWrapping="Wrap"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </DockPanel>
                </Border>

                <!-- Splitter -->
                <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Height="4"/>

                <!-- Answer/turn panel -->
                <Border Grid.Row="2" Background="{DynamicResource Brush.Background.Panel}">
                    <ScrollViewer Margin="8">
                        <ItemsControl ItemsSource="{Binding ConversationTurn.Turns}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,8"
                                            AutomationProperties.AutomationId="TurnCard"
                                            Background="{DynamicResource Brush.Background.Card}"
                                            BorderBrush="{DynamicResource Brush.Border}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            ClipToBounds="True">
                                        <DockPanel>
                                            <Rectangle DockPanel.Dock="Left"
                                                       Width="3"
                                                       Fill="{DynamicResource Brush.Accent}"/>
                                            <StackPanel Margin="6">
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                    <TextBlock Text="&#10067; "
                                                               Foreground="{DynamicResource Brush.Semantic.Question}"
                                                               FontSize="{DynamicResource Font.XS}"/>
                                                    <TextBlock Text="{Binding InitialQuestion}"
                                                               Foreground="{DynamicResource Brush.Semantic.Question}"
                                                               FontSize="{DynamicResource Font.SM}"
                                                               FontWeight="SemiBold"
                                                               TextWrapping="Wrap"/>
                                                </StackPanel>
                                                <!-- Status line: color-coded by StatusKind + busy indicator -->
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                    <TextBlock Text="{Binding StatusLabel}"
                                                               FontSize="{DynamicResource Font.XS}"
                                                               VerticalAlignment="Center">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Foreground"
                                                                        Value="{DynamicResource Brush.Foreground.Muted}"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding StatusKind}" Value="Busy">
                                                                        <Setter Property="Foreground"
                                                                                Value="{DynamicResource Brush.Semantic.Busy}"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding StatusKind}" Value="Awaiting">
                                                                        <Setter Property="Foreground"
                                                                                Value="{DynamicResource Brush.Semantic.Question}"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding StatusKind}" Value="Ready">
                                                                        <Setter Property="Foreground"
                                                                                Value="{DynamicResource Brush.Semantic.Active}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <ProgressBar IsIndeterminate="True"
                                                                 AutomationProperties.AutomationId="TurnCard_BusyIndicator"
                                                                 Width="36" Height="3"
                                                                 Margin="6,0,0,0"
                                                                 VerticalAlignment="Center"
                                                                 Background="Transparent"
                                                                 BorderThickness="0"
                                                                 Foreground="{DynamicResource Brush.Semantic.Busy}"
                                                                 Visibility="{Binding IsBusy,
                                                                     Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </StackPanel>
                                                <!-- Refining-vs-new affordance: which version is showing + when -->
                                                <TextBlock FontSize="{DynamicResource Font.XS}"
                                                           Foreground="{DynamicResource Brush.Foreground.Muted}"
                                                           Margin="0,0,0,4">
                                                    <Run Text="{Binding DisplayedVersion.VersionLabel, Mode=OneWay}"/><Run Text=" · "/><Run Text="{Binding DisplayedVersion.TimeLabel, Mode=OneWay}"/>
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding DisplayedVersion}" Value="{x:Null}">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <Grid Margin="0,0,0,4">
                                                    <!-- Streaming: live raw mono text -->
                                                    <TextBlock Text="{Binding DisplayedVersion.Text}"
                                                               FontSize="{Binding DataContext.ConversationTurn.AnswerFontSize,
                                                                                  RelativeSource={RelativeSource AncestorType=Window}}"
                                                               TextWrapping="Wrap"
                                                               FontFamily="Cascadia Mono, Consolas"
                                                               Foreground="{DynamicResource Brush.Foreground.Primary}">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding DisplayedVersion.IsComplete}" Value="False"/>
                                                                            <Condition Binding="{Binding DisplayedVersion.IsError}" Value="False"/>
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </MultiDataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>

                                                    <!-- Complete: rendered markdown -->
                                                    <controls:MarkdownPresenter
                                                        Markdown="{Binding DisplayedVersion.RenderedMarkdown}"
                                                        BaseFontSize="{Binding DataContext.ConversationTurn.AnswerFontSize,
                                                                               RelativeSource={RelativeSource AncestorType=Window}}">
                                                        <controls:MarkdownPresenter.Style>
                                                            <Style TargetType="controls:MarkdownPresenter">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding DisplayedVersion.IsComplete}" Value="True"/>
                                                                            <Condition Binding="{Binding DisplayedVersion.IsError}" Value="False"/>
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </MultiDataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </controls:MarkdownPresenter.Style>
                                                    </controls:MarkdownPresenter>

                                                    <!-- Error: distinct error styling -->
                                                    <TextBlock Text="{Binding DisplayedVersion.Text}"
                                                               FontSize="{Binding DataContext.ConversationTurn.AnswerFontSize,
                                                                                  RelativeSource={RelativeSource AncestorType=Window}}"
                                                               TextWrapping="Wrap"
                                                               FontFamily="Cascadia Mono, Consolas"
                                                               Foreground="{DynamicResource Brush.Semantic.Error}"
                                                               Visibility="{Binding DisplayedVersion.IsError,
                                                                   Converter={StaticResource BoolToVisibilityConverter}}"/>
                                                </Grid>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="Copy"
                                                            Command="{Binding DataContext.ConversationTurn.CopyLatestCommand,
                                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource ActionBtn}"/>
                                                    <Button Content="Regen"
                                                            Command="{Binding DataContext.ConversationTurn.RegenerateCommand,
                                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource ActionBtn}"/>
                                                    <Button Content="Dismiss"
                                                            Command="{Binding DataContext.ConversationTurn.DismissCommand,
                                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource ActionBtn}"/>
                                                    <Button Content="Resolve"
                                                            Command="{Binding DataContext.ConversationTurn.ResolveCommand,
                                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"
                                                            Style="{StaticResource ActionBtn}"/>
                                                    <Button Content="&#8722;"
                                                            Command="{Binding DataContext.ConversationTurn.DecreaseFontSizeCommand,
                                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                                            Style="{StaticResource ActionBtn}"
                                                            ToolTip="Decrease font size"/>
                                                    <Button Content="+"
                                                            Command="{Binding DataContext.ConversationTurn.IncreaseFontSizeCommand,
                                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                                            Style="{StaticResource ActionBtn}"
                                                            ToolTip="Increase font size"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal"
                                                            Margin="0,4,0,0"
                                                            Visibility="{Binding DataContext.ConversationTurn.IsFollowUpEnabled,
                                                                RelativeSource={RelativeSource AncestorType=Window},
                                                                Converter={StaticResource BoolToVisibilityConverter}}">
                                                    <TextBox Text="{Binding FollowUpText, UpdateSourceTrigger=PropertyChanged}"
                                                             Width="160"
                                                             FontSize="{DynamicResource Font.XS}">
                                                        <TextBox.InputBindings>
                                                            <KeyBinding Key="Return"
                                                                        Command="{Binding DataContext.ConversationTurn.SubmitFollowUpCommand,
                                                                            RelativeSource={RelativeSource AncestorType=Window}}"
                                                                        CommandParameter="{Binding}"/>
                                                        </TextBox.InputBindings>
                                                    </TextBox>
                                                    <Button Content="→"
                                                            Style="{StaticResource ActionBtn}"
                                                            Command="{Binding DataContext.ConversationTurn.SubmitFollowUpCommand,
                                                                RelativeSource={RelativeSource AncestorType=Window}}"
                                                            CommandParameter="{Binding}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </Grid>

            <!-- History panel — starts collapsed -->
            <ctrl:HistoryPanel x:Name="HistoryPanelControl"
                               AutomationProperties.AutomationId="Panel_History"
                               Visibility="Collapsed"/>
        </Grid>
    </DockPanel>
</Window>
