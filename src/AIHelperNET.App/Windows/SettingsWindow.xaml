<Window x:Class="AIHelperNET.App.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AIHelperNET.App.Windows"
        Title="AIHelper — Settings" Width="560" Height="600"
        WindowStartupLocation="Manual" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Background.Window}"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown">

    <DockPanel Margin="12">
        <Button DockPanel.Dock="Bottom"
                Content="Save"
                HorizontalAlignment="Right"
                Width="80" Margin="0,8,0,0"
                AutomationProperties.AutomationId="Btn_SaveSettings"
                Command="{Binding SaveSettingsCommand}"/>

        <TabControl>
            <!-- ═══ API Key ═══ -->
            <TabItem Header="API Key">
                <StackPanel Margin="8">
                    <TextBlock Text="Claude API Key" FontWeight="SemiBold"
                               FontSize="{DynamicResource Font.SM}"
                               Foreground="{DynamicResource Brush.Foreground.Primary}"
                               Margin="0,0,0,6"/>
                    <DockPanel>
                        <Button Content="Delete" DockPanel.Dock="Right" Width="60"
                                Foreground="#CC4444"
                                Command="{Binding DeleteApiKeyCommand}"/>
                        <Button Content="Save" DockPanel.Dock="Right" Width="60" Margin="0,0,6,0"
                                Command="{Binding SaveApiKeyCommand}"/>
                        <Border Background="{DynamicResource Brush.Background.Panel}"
                                BorderBrush="{DynamicResource Brush.Border}"
                                BorderThickness="1" CornerRadius="3" Padding="1">
                            <PasswordBox x:Name="ApiKeyBox"
                                         Background="Transparent"
                                         Foreground="{DynamicResource Brush.Foreground.Primary}"
                                         CaretBrush="{DynamicResource Brush.Foreground.Primary}"
                                         BorderThickness="0" Padding="5,3"
                                         PasswordChanged="ApiKeyBox_PasswordChanged"/>
                        </Border>
                    </DockPanel>
                    <TextBlock Text="{Binding StatusMessage}"
                               Foreground="{DynamicResource Brush.Semantic.Active}"
                               Margin="0,8,0,0"
                               FontSize="{DynamicResource Font.SM}"/>
                    <TextBlock Text="AI Backend" FontWeight="SemiBold"
                               FontSize="{DynamicResource Font.SM}"
                               Foreground="{DynamicResource Brush.Foreground.Primary}"
                               Margin="0,16,0,6"/>
                    <RadioButton Style="{StaticResource RadioBtn}" Content="Claude" GroupName="Backend"
                                 AutomationProperties.AutomationId="Backend_Claude"
                                 IsChecked="{Binding ActiveBackend,
                                     Converter={StaticResource EnumToBoolConverter},
                                     ConverterParameter=Claude}"/>
                    <RadioButton Style="{StaticResource RadioBtn}" Content="Ollama (local)" GroupName="Backend"
                                 AutomationProperties.AutomationId="Backend_Ollama"
                                 IsChecked="{Binding ActiveBackend,
                                     Converter={StaticResource EnumToBoolConverter},
                                     ConverterParameter=Ollama}"/>
                    <TextBlock Text="Answer model (Claude)" FontWeight="SemiBold"
                               FontSize="{DynamicResource Font.SM}"
                               Foreground="{DynamicResource Brush.Foreground.Primary}"
                               Margin="0,16,0,6"/>
                    <ComboBox ItemsSource="{Binding Source={x:Static local:EnumValues.AnswerModels}}"
                              DisplayMemberPath="Display"
                              SelectedValuePath="Value"
                              SelectedValue="{Binding AnswerModel}"
                              AutomationProperties.AutomationId="AnswerModelCombo"
                              Margin="0,0,0,4"/>
                    <TextBlock Text="Faster models answer sooner; Opus is highest quality."
                               TextWrapping="Wrap"
                               FontSize="{DynamicResource Font.SM}"
                               Foreground="{DynamicResource Brush.Foreground.Secondary}"/>
                </StackPanel>
            </TabItem>

            <!-- ═══ Audio ═══ -->
            <TabItem Header="Audio">
                <StackPanel Margin="8">
                    <TextBlock Text="AUDIO DEVICES" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                    <TextBlock Text="Microphone" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="MicCombo"
                              DisplayMemberPath="FriendlyName"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding SelectedMicDeviceId}"
                              Margin="0,0,0,8"/>
                    <TextBlock Text="System Audio (Loopback)" Style="{StaticResource FieldLabel}"/>
                    <ComboBox x:Name="LoopbackCombo"
                              DisplayMemberPath="FriendlyName"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding SelectedLoopbackDeviceId}"
                              Margin="0,0,0,16"/>
                    <TextBlock Text="TRANSCRIPTION" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                    <TextBlock Text="Model" Style="{StaticResource FieldLabel}"/>
                    <ComboBox ItemsSource="{Binding Source={x:Static local:EnumValues.WhisperModelSizes}}"
                              DisplayMemberPath="Display"
                              SelectedValuePath="Value"
                              SelectedValue="{Binding WhisperModel}"
                              Margin="0,0,0,8"/>
                    <TextBlock Text="Language" Style="{StaticResource FieldLabel}"/>
                    <ComboBox SelectedValue="{Binding WhisperLanguage}"
                              SelectedValuePath="Tag">
                        <ComboBoxItem Content="Auto-detect" Tag="auto"/>
                        <ComboBoxItem Content="English"     Tag="en"/>
                        <ComboBoxItem Content="Russian"     Tag="ru"/>
                        <ComboBoxItem Content="Polish"      Tag="pl"/>
                    </ComboBox>
                    <StackPanel Margin="0,8,0,0">
                        <CheckBox Content="Bias transcription with tech glossary"
                                  IsChecked="{Binding GlossaryEnabled}"
                                  Foreground="{DynamicResource Brush.Foreground.Primary}"
                                  AutomationProperties.Name="Enable transcription glossary"/>
                        <ItemsControl ItemsSource="{Binding GlossaryDomains}" Margin="16,4,0,0"
                                      IsEnabled="{Binding GlossaryEnabled}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding DisplayName}"
                                              IsChecked="{Binding IsEnabled}"
                                              Foreground="{DynamicResource Brush.Foreground.Primary}"
                                              AutomationProperties.Name="{Binding DisplayName}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
            </TabItem>

            <!-- ═══ Code Profiles ═══ -->
            <TabItem Header="Code Profiles">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <DockPanel Margin="0,0,0,8">
                            <Button Content="Delete" DockPanel.Dock="Right" Width="55" Margin="4,0,0,0"
                                    Command="{Binding DeletePresetCommand}"
                                    CommandParameter="{Binding SelectedPreset}"/>
                            <Button Content="Load"   DockPanel.Dock="Right" Width="55" Margin="4,0,0,0"
                                    Command="{Binding LoadPresetCommand}"
                                    CommandParameter="{Binding SelectedPreset}"/>
                            <ComboBox ItemsSource="{Binding Presets}"
                                      SelectedItem="{Binding SelectedPreset}"
                                      DisplayMemberPath="Name"/>
                        </DockPanel>
                        <Separator Margin="0,0,0,8"/>
                        <TextBlock Text="TECH STACK" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                        <TextBlock Text="Language"       Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding ProgrammingLanguage}" Margin="0,0,0,4"/>
                        <TextBlock Text="Backend"        Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding BackendFramework}"    Margin="0,0,0,4"/>
                        <TextBlock Text="Frontend"       Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding FrontendFramework}"   Margin="0,0,0,4"/>
                        <TextBlock Text="Database"       Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding Database}"            Margin="0,0,0,4"/>
                        <TextBlock Text="Cloud / DevOps" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding CloudDevOps}"         Margin="0,0,0,4"/>
                        <TextBlock Text="Messaging"      Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding Messaging}"           Margin="0,0,0,4"/>
                        <TextBlock Text="Architecture"   Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding ArchitectureStyle}"   Margin="0,0,0,4"/>
                        <TextBlock Text="Testing"        Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding TestingFramework}"    Margin="0,0,0,4"/>
                        <TextBlock Text="Notes"          Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding CustomNotes}"         Margin="0,0,0,12" Height="48"
                                 AcceptsReturn="True" TextWrapping="Wrap"/>
                        <TextBlock Text="ANSWER STYLE" Style="{StaticResource SectionLabel}" Margin="0,0,0,6"/>
                        <TextBlock Text="Length"         Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding Source={x:Static local:EnumValues.AnswerLengths}}"
                                  DisplayMemberPath="Display"
                                  SelectedValuePath="Value"
                                  SelectedValue="{Binding AnswerLength}"
                                  Margin="0,0,0,4"/>
                        <TextBlock Text="Max answer length (tokens)" Style="{StaticResource FieldLabel}"/>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="200"
                                       Foreground="{DynamicResource Brush.Foreground.Muted}"
                                       FontSize="{DynamicResource Font.XS}" Margin="0,0,6,0"/>
                            <Slider Grid.Column="1"
                                    Minimum="200" Maximum="4000"
                                    SmallChange="50" LargeChange="200"
                                    TickFrequency="50" IsSnapToTickEnabled="True"
                                    AutomationProperties.AutomationId="Settings_MaxAnswerTokens"
                                    Value="{Binding MaxAnswerTokens}"/>
                            <TextBlock Grid.Column="2" Text="4000"
                                       Foreground="{DynamicResource Brush.Foreground.Muted}"
                                       FontSize="{DynamicResource Font.XS}" Margin="6,0,0,0"/>
                        </Grid>
                        <TextBlock Text="{Binding MaxAnswerTokens, StringFormat='{}{0} tokens'}"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource Brush.Foreground.Secondary}"
                                   FontSize="{DynamicResource Font.XS}" Margin="0,0,0,8"/>
                        <TextBlock Text="Answer-latest window (seconds)" Style="{StaticResource FieldLabel}"/>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="30"
                                       Foreground="{DynamicResource Brush.Foreground.Muted}"
                                       FontSize="{DynamicResource Font.XS}" Margin="0,0,6,0"/>
                            <Slider Grid.Column="1"
                                    Minimum="30" Maximum="300"
                                    SmallChange="10" LargeChange="30"
                                    TickFrequency="10" IsSnapToTickEnabled="True"
                                    AutomationProperties.AutomationId="Settings_LatestQuestionWindow"
                                    Value="{Binding LatestQuestionWindowSeconds}"/>
                            <TextBlock Grid.Column="2" Text="300"
                                       Foreground="{DynamicResource Brush.Foreground.Muted}"
                                       FontSize="{DynamicResource Font.XS}" Margin="6,0,0,0"/>
                        </Grid>
                        <TextBlock Text="{Binding LatestQuestionWindowSeconds, StringFormat='{}{0} s'}"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource Brush.Foreground.Secondary}"
                                   FontSize="{DynamicResource Font.XS}" Margin="0,0,0,8"/>
                        <TextBlock Text="Complexity"     Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding Source={x:Static local:EnumValues.AnswerComplexities}}"
                                  DisplayMemberPath="Display"
                                  SelectedValuePath="Value"
                                  SelectedValue="{Binding AnswerComplexity}"
                                  Margin="0,0,0,4"/>
                        <TextBlock Text="Style"          Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding Source={x:Static local:EnumValues.AnswerStyles}}"
                                  DisplayMemberPath="Display"
                                  SelectedValuePath="Value"
                                  SelectedValue="{Binding AnswerStyle}"
                                  Margin="0,0,0,4"/>
                        <TextBlock Text="Output Language" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding OutputLanguage}" Margin="0,0,0,12"/>
                        <Separator Margin="0,0,0,8"/>
                        <DockPanel>
                            <Button Content="Update current" DockPanel.Dock="Right" Margin="4,0,0,0"
                                    Command="{Binding UpdateCurrentPresetCommand}"/>
                            <TextBox Text="{Binding PresetName}" Margin="0,0,4,0"/>
                        </DockPanel>
                        <Button Content="Save as new preset" HorizontalAlignment="Left"
                                Margin="0,4,0,0"
                                Command="{Binding SaveAsNewPresetCommand}"/>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ═══ Appearance ═══ -->
            <TabItem Header="Appearance">
                <StackPanel Margin="8">
                    <TextBlock Text="OVERLAY" Style="{StaticResource SectionLabel}" Margin="0,0,0,8"/>
                    <TextBlock Text="Transparency" Style="{StaticResource FieldLabel}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Solid"
                                   Foreground="{DynamicResource Brush.Foreground.Muted}"
                                   FontSize="{DynamicResource Font.XS}" Margin="0,0,6,0"/>
                        <!-- Bound to OverlayOpacity (0.2–1.0); reversed so dragging right = more see-through. -->
                        <Slider Grid.Column="1"
                                Minimum="0.2" Maximum="1.0" SmallChange="0.05"
                                IsDirectionReversed="True"
                                Value="{Binding OverlayOpacity}"/>
                        <TextBlock Grid.Column="2" Text="See-through"
                                   Foreground="{DynamicResource Brush.Foreground.Muted}"
                                   FontSize="{DynamicResource Font.XS}" Margin="6,0,0,0"/>
                    </Grid>
                    <TextBlock Text="{Binding OverlayTransparency, StringFormat='{}{0:P0} transparent'}"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource Brush.Foreground.Secondary}"
                               FontSize="{DynamicResource Font.SM}" Margin="0,4,0,0"/>

                    <TextBlock Text="MODE" Style="{StaticResource SectionLabel}" Margin="0,16,0,6"/>
                    <RadioButton GroupName="OverlayMode" Style="{StaticResource RadioBtn}"
                                 Content="Stealth — hidden from screen capture"
                                 AutomationProperties.AutomationId="OverlayMode_Stealth"
                                 IsChecked="{Binding OverlayMode,
                                     Converter={StaticResource EnumToBoolConverter},
                                     ConverterParameter=Stealth}"/>
                    <RadioButton GroupName="OverlayMode" Style="{StaticResource RadioBtn}"
                                 Content="See-through — real translucency"
                                 AutomationProperties.AutomationId="OverlayMode_SeeThrough"
                                 IsChecked="{Binding OverlayMode,
                                     Converter={StaticResource EnumToBoolConverter},
                                     ConverterParameter=SeeThrough}"/>
                    <TextBlock TextWrapping="Wrap" FontSize="{DynamicResource Font.XS}" Margin="0,6,0,0"
                               Foreground="{DynamicResource Brush.Semantic.Question}"
                               Visibility="{Binding IsSeeThroughMode, Converter={StaticResource BoolToVisibilityConverter}}"
                               Text="⚠ See-through uses real transparency, so the overlay is NOT hidden from screen sharing or recording. Stealth is unavailable in this mode."/>
                    <TextBlock Text="Changing mode takes effect after an app restart."
                               TextWrapping="Wrap" FontSize="{DynamicResource Font.XS}" Margin="0,6,0,0"
                               Foreground="{DynamicResource Brush.Foreground.Muted}"/>
                </StackPanel>
            </TabItem>

            <!-- ═══ Shortcuts ═══ -->
            <TabItem Header="Shortcuts">
                <StackPanel Margin="8">
                    <TextBlock Text="KEYBOARD SHORTCUTS" Style="{StaticResource SectionLabel}" Margin="0,0,0,4"/>
                    <TextBlock Text="Global shortcuts work even when the overlay isn't focused."
                               Foreground="{DynamicResource Brush.Foreground.Muted}"
                               FontSize="{DynamicResource Font.XS}" Margin="0,0,0,10"
                               TextWrapping="Wrap"/>
                    <ItemsControl ItemsSource="{Binding HotkeyRows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Description}"
                                                   Foreground="{DynamicResource Brush.Foreground.Primary}"
                                                   FontSize="{DynamicResource Font.SM}"
                                                   VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <CheckBox Content="Ctrl"  IsChecked="{Binding Ctrl}"  VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <CheckBox Content="Shift" IsChecked="{Binding Shift}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <CheckBox Content="Alt"   IsChecked="{Binding Alt}"   VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <CheckBox Content="Win"   IsChecked="{Binding Win}"   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <ComboBox ItemsSource="{Binding KeyChoices}"
                                                      SelectedValue="{Binding SelectedKey}"
                                                      SelectedValuePath="Key" DisplayMemberPath="Display"
                                                      Width="64" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <Button Content="Record"
                                                    Command="{Binding DataContext.StartRecordingCommand,
                                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <Button Content="Reset"
                                                    Command="{Binding DataContext.ResetRowCommand,
                                                              RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                    <TextBlock Text="Press the shortcut keys…"
                                               Foreground="{DynamicResource Brush.Semantic.Active}"
                                               FontSize="{DynamicResource Font.XS}" Margin="0,2,0,0"
                                               Visibility="{Binding IsRecording,
                                                   Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource Brush.Semantic.Error}"
                                               FontSize="{DynamicResource Font.XS}" Margin="0,2,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Button Content="Reset all to defaults"
                            Command="{Binding ResetAllHotkeysCommand}"
                            HorizontalAlignment="Left" Margin="0,4,0,0"/>
                </StackPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
